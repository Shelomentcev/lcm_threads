#include<iostream>
#include"monitor.hpp"

int main() {
	Monitor();
	
	//std::cout << "Hardware concurrency: " << std::thread::hardware_concurrency() << std::endl;
	//std::map<uint64_t, uint64_t> res = prime_table(765);
	system("pause");
	return 0;
}
